def zeroMatrix(arr):

    toMod = []    # even i, odd j

    for i in range(len(arr)):   # set all of the indexes that have 0
        for j in range(len(arr[i])):
            if arr[i][j] == 0:
                toMod.append(i)
                toMod.append(j)

    while toMod:
        for i in range(len(arr[0])):    # modify x axis
            arr[toMod[0]][i] = 0

        for i in range(len(arr)):       # modify y axis
            arr[i][toMod[1]] = 0
        del toMod[0:2]

    for i in range(len(arr)):
        print(arr[i])


zeroMatrix([[1, 2, 3, 4, 5, 6],
            [6, 0, 8, 9, 1, 1],
            [1, 2, 3, 4, 5, 6],
            [6, 7, 8, 9, 0, 2],
            [1, 2, 3, 4, 5, 6]])
